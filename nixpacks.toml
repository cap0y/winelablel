[phases.setup]
nixPkgs = ["nodejs_20", "python311", "pkg-config", "pixman", "cairo", "pango", "libjpeg"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
